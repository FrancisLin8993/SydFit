name: Deploy SydFit API
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v3
        with:
          service: sydfit-api
          region: australia-southeast1
          source: .
          flags: >-
            --allow-unauthenticated
            --min-instances=0
            --max-instances=1
            --service-account=github-actions-sydfit-api@personal-assistant-mem0-499203.iam.gserviceaccount.com
          env_vars: |
            OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
            OPENAI_MODEL=gpt-5.4-mini
            BARK_DEVICE_KEY=${{ secrets.BARK_DEVICE_KEY }}
            BARK_SERVER_URL=https://api.day.app
            BARK_GROUP=Weather
            BARK_LEVEL=active
            SCHEDULE_TIMEZONE=Australia/Sydney
            MCP_SERVER_URL=${{ secrets.MCP_SERVER_URL }}
            MCP_ACCESS_TOKEN=${{ secrets.MCP_ACCESS_TOKEN }}
            MEM0_ACCESS_TOKEN=${{ secrets.MEM0_ACCESS_TOKEN }}
            MEM0_API_URL=${{ secrets.MEM0_API_URL }}
            SYDFIT_API_KEY=${{ secrets.SYDFIT_API_KEY }}