{
  "name": "sydfit-weather-bark",
  "version": "1.0.0",
  "description": "Daily Mascot, NSW weather clothing recommendation sent to iPhone via Bark.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "run-once": "node --env-file=.env src/index.js --force",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/weather.js && node --check src/openai.js && node --check src/bark.js && node --check src/scheduler.js && node --check src/trafficAgent.js",
    "test": "node --test"
  },
  "dependencies": {
    "openai": "^6.42.0"
  }
}
